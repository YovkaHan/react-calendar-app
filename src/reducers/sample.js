/**
 * Created by Jordan3D on 5/6/2018.
 */
const initialSet = 'week';

export const sample = (state = initialSet, action) => {
  switch (action.type) {
    // case SELECT_SET :
    //   return setObjectConstruct(action.set);
    default:
      return state
  }
};
